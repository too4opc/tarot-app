[build]
  command = "npm run build"
  publish = "dist"

# /api/tarot-ai ถูก serve โดย netlify/edge-functions/tarot-ai.js โดยตรง (กำหนด path ในตัวไฟล์เองผ่าน
# `export const config = { path: "/api/tarot-ai" }`) ไม่ต้องประกาศ redirect หรือ [functions] เพิ่มแล้ว
